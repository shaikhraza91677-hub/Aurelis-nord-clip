import { NextResponse } from 'next/server';
import { createHmac, timingSafeEqual } from 'node:crypto';
import { PrismaClient } from '@prisma/client';
const globalForPrisma=globalThis as typeof globalThis & {prisma?:PrismaClient};const prisma=globalForPrisma.prisma??new PrismaClient();if(process.env.NODE_ENV!=='production')globalForPrisma.prisma=prisma;
function verify(raw:string,header:string,secret:string){const parts=Object.fromEntries(header.split(',').map(x=>x.split('=')));const ts=parts.t;const sig=parts.v1;if(!ts||!sig)return false;if(Math.abs(Date.now()/1000-Number(ts))>300)return false;const expected=createHmac('sha256',secret).update(`${ts}.${raw}`).digest('hex');return expected.length===sig.length&&timingSafeEqual(Buffer.from(expected),Buffer.from(sig));}
export async function POST(req:Request){const secret=process.env.STRIPE_WEBHOOK_SECRET;if(!secret)return new NextResponse('webhook not configured',{status:503});const raw=await req.text();const signature=req.headers.get('stripe-signature')||'';if(!verify(raw,signature,secret))return new NextResponse('invalid signature',{status:400});const event=JSON.parse(raw);const obj=event.data?.object||{};const userId=obj.client_reference_id||obj.metadata?.userId||null;if(event.type==='checkout.session.completed'&&userId){await prisma.user.update({where:{id:userId},data:{plan:'pro',subscriptionStatus:'active',stripeCustomerId:typeof obj.customer==='string'?obj.customer:undefined}}).catch(()=>undefined);}if((event.type==='customer.subscription.deleted'||event.type==='customer.subscription.updated')&&typeof obj.customer==='string'){const status=String(obj.status||'');await prisma.user.updateMany({where:{stripeCustomerId:obj.customer},data:{plan:status==='active'||status==='trialing'?'pro':'free',subscriptionStatus:status}});}return NextResponse.json({received:true});}
